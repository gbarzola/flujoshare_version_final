	<div class="container">
		<p class="comment_name"><strong><%= comment.nombre%></strong></p>
		<p class="comment_body"><%= comment.comentario %></p>
		<p class="comment_time">Publicado hace <%= time_ago_in_words(comment.created_at) %></p>
	</div>

